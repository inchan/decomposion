eval_version: planning-eval-v1-dev
case_id: example
reference:
  nodes:
    - id: permission_policy
      kind: decision
      concept: permission policy
      severity: critical
      required: true
      acceptable_aliases: []
    - id: retrieval_authorization
      kind: task
      concept: retrieval authorization
      severity: critical
      required: true
      acceptable_aliases:
        - authorize retrieval against document permissions
  edges:
    - from: permission_policy
      to: retrieval_authorization
      relation: requires_decision
      severity: critical
      required: true
  unknowns:
    - concept: cache existence
      expected_state: unknown
      severity: major
notes:
  - This is an illustrative contract, not a Golden answer.
  - Semantic equivalence and granularity require semantic/human adjudication.
